#include <iostream>

class Circle{
    public:
        Circle(double r):
            radius(r)
        {
        }
        double re1(){
            return (1);
        }
    private:
        double radius;
};

int main()
{
    Circle circle(5.0);
    std::cout << circle.re1() << endl;

    new_node = new Node{q,nullptr,nullptr}; // new_node points to a new node alpha.
    new_node-> data = q; // assign value 1 to the node alpha.
    new_node -> prev = nullptr; // prev pointer in alpha points to nothingness
    new_node -> next = nullptr;  //next pointer in aplha points to nothingness

}
